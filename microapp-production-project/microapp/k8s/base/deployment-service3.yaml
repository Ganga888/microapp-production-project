apiVersion: apps/v1
kind: Deployment
metadata:
  name: service3
  namespace: microapp
  labels:
    app: service3
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service3
  template:
    metadata:
      labels:
        app: service3
        version: v1
    spec:
      containers:
        - name: service3
          image: YOUR_DOCKERHUB_USERNAME/service3:v1
          ports:
            - containerPort: 5000
          env:
            - name: SERVICE_NAME
              value: "service3"
            - name: VERSION
              value: "v1"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /readyz
              port: 5000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: service3
  namespace: microapp
spec:
  selector:
    app: service3
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP
